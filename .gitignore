# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
venv/
env/
.venv/
*.egg-info/
dist/
build/

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Runtime
data/
*.db
*.db-journal
*.db-wal
*.db-shm
*.log
nohup.out
logs/
data/archive/

# Local config overrides（保留 settings.example.json；settings.json 含使用者實際值）
.env
config/settings.json
config/ota_token
config/ota_backup/

# OTA 推送暫存 manifest（本機一次性，不進版控）
ota_manifest_*.json

# 本機驗收產物（smoke test screenshots）
screenshots/

# Smoke test 產生的 SQLite / 暫存檔（這些 *.db 本來就被 *.db rule 蓋掉，下面明確記上）
# playwright 預設產出位置
test-results/
playwright-report/
.trash_archive_*

# 軟刪除歸檔目錄（AGENTS.md 政策：禁止 rm -rf，用 mv 進 .trash/）
.trash/
